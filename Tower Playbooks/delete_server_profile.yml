# This Playbook is to run an additional playbook within the roles folder:
#   - Delete a Server Profile
#
#
# Created by thopper91 (2017)
---
- hosts: all
  connection: local
  gather_facts: True
  roles:
    - {role: Delete_Server_Profile}
